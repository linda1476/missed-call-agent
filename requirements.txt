# Core is stdlib-only; these are needed for the HTTP tool server, hosted demo,
# and offline STT acceptance tests.
pytest>=8.0
fastapi>=0.115
uvicorn>=0.30
websockets>=13.0
faster-whisper>=1.1
edge-tts>=7.0
jsonschema>=4.23
httpx>=0.27
